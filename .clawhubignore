# ── Secrets: never ship the user's real API key with the published package ──
# OpenAlex key lives in .env (gitignored). ClawHub packaging must NOT transfer it.
.env
.env.*
!.env.example

# ── Local injection / build metadata (machine-specific, never publish) ──
.ctbase_injected.json
*.ctbase_bak_*

# ── Local build / test artifacts (keep the package lean) ──
out_*/
# 测试内容（源码 + 夹具 + 运行产物）一律不进发布包（ct-base §16.8 红线，2026-08-13）
tests/
scripts/tests/
__pycache__/
*.pyc
# 一次性开发工具（迁移/批量修复脚本，非技能运行所需，不进发布包）
tools/
