# OpenAlex API key（免费申请：https://openalex.org/settings/api）
# 使用：复制本文件为 .env，把下面的值替换成你的真实 key（明文或 XOR+base64 混淆值皆可，脚本自动识别）。
# 技能会自动读取并解码（无需每次传 --openalex-key），明文 key 不会出现在日志中。
OPENALEX_API_KEY=your_openalex_api_key_here
